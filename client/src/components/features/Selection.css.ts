import { style } from "@vanilla-extract/css";
import { vars } from "../../styles/themes.css";


export const ProductSelectParent = style({

})

export const ProductSelectDiv = style({
    display: 'flex',
    margin: '50px',
    padding: '50px',
})

export const ProductSelectContent = style({
    margin: '0 auto'
})