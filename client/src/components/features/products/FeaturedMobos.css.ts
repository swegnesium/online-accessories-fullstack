import { vars } from "../../../styles/themes.css";
import { style } from "@vanilla-extract/css";

export const Heading = style({
    color: `${vars.colors.complementary}`
})

export const motherboardDiv = style({

})